{
  "name": "codesandbox-importers",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:git-extractor":
      "lerna run build --scope codesandbox-import-utils --scope types --stream && lerna run build --scope git-converter",
    "test": "lerna run test",
    "start": "lerna run start --stream"
  },
  "author": "",
  "devDependencies": {
    "lerna": "^2.4.0"
  },
  "workspaces": [
    "packages/cli",
    "packages/git-extractor",
    "packages/import-utils",
    "packages/types"
  ]
}
